<div class="t">&nbsp;</div>
<div class="c">
	<div class="title">
		<h2><%= @title %></h2>
		<%= link_to 'featured', featured_events_path, :class => "view" %> 
		<%= link_to 'suggested', suggested_events_path, :class => "view" %> 				
		<%= link_to 'view all', events_path, :class => "view" %>
	</div><!--title-->
	<%= will_paginate(@events)%>
	<%= render :partial => 'event', :collection => @events %>
	<%= will_paginate(@events)%>	
</div>
<div class="b">&nbsp;</div>

<% content_for :sidebar do %>

	<%= render :partial => 'filter_by_tags' %>
<% end %>