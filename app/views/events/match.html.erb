<% if @events.blank? %>
	<h2> No matching events</h2>
	
	<p> Looks would like to proceed ahead and create the event. You can do so by clicking the link below </p>
	
	<h3><%= link_to 'Create Event', events_path(:event => params[:event], :other_causes=> params[:other_causes], :other_skills => params[:other_skills]), :method => :post %></h3>
<% else %>	
	<h2> There are <%= @events.length %> events matching</h2>
	
	<p> Its possible that someone among your friends might have created the event before you. You may look at the events listed below. If none of the
		events match you may go ahead and create the event.</p>
		
	<div id="events">
		<% for event in @events %>
		<div class="event">
			<h3><%= event.title %></h3>
			<p> Start Date: <%= event.from_date %>  End Date: <%= event.to %> </p>
			<p> Created by: <%= event.user %></p>
		</div>
		<% end %>
	</div>
	
	<p> If none of the events match your event, click the link below to create one.</p>
		<h3><%= link_to 'Create Event', events_path(:event => params[:event], :other_causes=> params[:other_causes], :other_skills => params[:other_skills]), :method => :post%></h3>
<% end %>