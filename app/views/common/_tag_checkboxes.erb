<% for item in items %>
	<%= check_box_tag item_name+'[]', item, object_items.include?(item) %> <%= item %><br />
<% end %>
Others(comma separated) <br />
<%= text_field_tag 'other_'+item_name, (object_items - items).join(',') %>